{
    "name": "gianlucagiacometti-bootstrap-select-build",
    "private": true,
    "description": "Build tooling for gianlucagiacometti/bootstrap-select assets.",
    "scripts": {
        "minify": "node scripts/minify-assets.js",
        "build": "npm run minify"
    },
    "devDependencies": {
        "clean-css": "^5",
        "terser": "^5"
    },
    "engines": {
        "node": ">=18"
    }
}
